generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Visit {
  id      Int    @id @default(autoincrement())
  count   Int
}
